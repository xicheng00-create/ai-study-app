# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.coverage
htmlcov/
.pytest_cache/

# Runtime data (never commit)
instance/
data/*.db
data/*.sqlite
*.sqlite*
chroma_db/
uploads/
files/

# Env & secrets
.env
.env.*
!.env.example

# Logs
*.log
logs/

# Mac
.DS_Store

# 内部工作日志（非项目产物）
.workbuddy/

# 课程原始素材（554MB，走 iCloud Drive 备份，不进 git）
课件/
audit/

# 本地库备份（含学生数据，绝不进 git）
backups/

# ── 受保护文档（2026-09-22 归位：移出 git 跟踪 + 显式忽略）────────────────────
# 政策：这些是「设计/执行方案 + 原型稿 + 课程总路径源文档 + 生产启动脚本」，
# 历史上曾被误跟踪（审计 GAP-1，commit fa10f69/0c32f01「补入历史执行方案与设计稿」）。
# 现按 runbook 口径归位为「受保护未跟踪」，并在此显式忽略 —— 从此 `git add -A`
# 也不可能把它们重新加回索引（机械保护，不靠人记纪律）。
# 注：文件与内容全部保留在盘上；历史提交里仍有副本可回溯。
ChangeRequest-*.md
DesignSpec-*-执行方案.md
ui-design-*.html
deploy/aistudy-run-port.sh
小白AI课程*学习路径.md
